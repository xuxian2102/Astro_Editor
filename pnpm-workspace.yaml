allowBuilds:
  # 嵌入式 Tauri WebDriver 不使用浏览器驱动下载器；禁止它们的安装脚本。
  edgedriver: false
  geckodriver: false
  # Vite/WDIO 都需要本机 esbuild 二进制，允许其校验安装。
  esbuild: true
minimumReleaseAgeExclude:
  # 精确锁定并显式审阅的测试服务；生产依赖不在豁免范围内。
  - '@wdio/tauri-service@1.3.0'
